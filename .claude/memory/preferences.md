---
name: preferences
description: 用户编码偏好 — 最小化修改，不加多余内容
metadata:
  type: user
---

- 只改目标代码行，不格式化无关代码，不顺手重构。
- 不加解释性注释，除非明确要求。
- diff 尽可能小，变量命名和空格缩进完全匹配已有风格。
- 不要过度工程化，能用简单的方案就用简单的方案。

**Why:** 用户多次要求「最小化修正 不要乱改内容 甚至加一堆注释」。
**How to apply:** 每次修改前问自己：改的每一行是否直接为功能服务？是 → 改；不是 → 删掉。
